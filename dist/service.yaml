apiVersion: v1
kind: Service
metadata:
  name: trend-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-subnets: subnet-04117aa9e3d99d6c8
spec:
  selector:
    app: trend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
